apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: frontend
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: adaptive-cache
spec:
  type: ClusterIP
  ports:
    - port: 8501
      targetPort: 8501
      name: http
      protocol: TCP
  selector:
    app: frontend
